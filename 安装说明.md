# 明日方舟基建桌宠安装说明

这是银灰与灵知的 Windows 桌面宠物。普通使用不需要安装 Node.js，也不需要联网下载依赖。

## 安装

1. 解压收到的 `完整安装包.zip`。
2. 打开解压后的 `完整安装包` 文件夹。
3. 双击运行 `Install-ArknightsBasePet.cmd`。
4. 安装完成后桌宠会自动启动，并创建桌面快捷方式和开始菜单快捷方式。

请保持 `Install-ArknightsBasePet.cmd`、`install.ps1`、`pet.zip` 在同一个文件夹内。不要直接运行旧版 `Setup.exe`。

## Windows SmartScreen 提示

如果 Windows 提示“目前无法访问 SmartScreen”或拦截运行，这是因为安装脚本没有商业代码签名。确认安装包来源可信后，可以选择继续运行。

常见处理方式：

1. 在提示窗口中选择 `更多信息`，再选择 `仍要运行`。
2. 如果没有看到 `仍要运行`，可以右键 `Install-ArknightsBasePet.cmd`，选择 `属性`，勾选或点击 `解除锁定`，然后再次运行。
3. 不确定来源时不要继续运行，先向发送者确认安装包来源。

## 运行

安装后可以用以下任一方式启动：

- 双击桌面快捷方式 `明日方舟基建桌宠`。
- 从开始菜单启动 `明日方舟基建桌宠`。
- 直接运行 `%LOCALAPPDATA%\ArknightsBasePet\ArknightsBasePet.exe`。

如果使用便携版，不安装也可以运行：

```text
明日方舟基建桌宠-portable\ArknightsBasePet.exe
```

## 基本操作

- `Ctrl+Alt+P`：暂停或继续自动行动。
- `Ctrl+Alt+H`：重新显示隐藏后的控制栏。
- 控制栏中的三杠菜单可以调节人物大小、人物距离、移动速度、活跃度、专注模式，也可以恢复默认设置。
- 自动状态下桌宠会尽量不挡住其他应用点击；暂停后可以尝试拖拽移动。

## 卸载

这个安装包使用脚本安装，不会出现在传统 `控制面板 - 程序和功能` 列表中。卸载时按下面做：

1. 退出桌宠。如果看不到控制栏，可以先按 `Ctrl+Alt+H` 显示控制栏，再点 `退出`。
2. 删除安装目录：

```text
%LOCALAPPDATA%\ArknightsBasePet
```

3. 删除桌面快捷方式：

```text
桌面\明日方舟基建桌宠.lnk
```

4. 删除开始菜单快捷方式：

```text
%APPDATA%\Microsoft\Windows\Start Menu\Programs\明日方舟基建桌宠.lnk
```

如果还想删除个人配置和诊断日志，再删除：

```text
%APPDATA%\arknights-base-desktop-pet
```

保留这个目录也可以，之后重新安装时会继续使用之前的设置。

## 配置和日志位置

普通使用不需要打开这些文件。如果桌宠出现闪烁、拖拽异常、动作异常或启动报错，可以让 AI 帮忙读取或打包这些路径。

- 配置文件：`%APPDATA%\arknights-base-desktop-pet\settings.json`
- 诊断日志目录：`%APPDATA%\arknights-base-desktop-pet\diagnostics`

诊断日志是 `.jsonl` 文件，文件名类似：

```text
render-diagnostics-*.jsonl
```

## macOS 支持

当前安装包只面向 Windows。macOS 不能直接运行这个 `.cmd` 安装包，也不能直接运行 Windows 版 `.exe`。Electron 项目理论上可以另做 macOS 适配和打包，但需要单独处理打包脚本、窗口行为和安装方式。
