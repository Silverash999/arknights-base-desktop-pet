# 桌宠素材包格式说明

本文件定义“可替换干员”功能使用的本地素材包格式。素材包目录中必须有 `profile.json`；应用会将整个目录复制到：

```text
%APPDATA%\arknights-base-desktop-pet\profiles\<素材包 ID>\
```

## 最小目录

```text
my-operator-profile\
  profile.json
  default.png
  model.skel
  model.atlas
  build_char_172_svrash_default.png
```

纹理 PNG 通常由 `model.atlas` 引用，名称必须与 atlas 文件中的引用一致。PRTS 自动下载会保留 atlas 内的原始 PNG 名称，不能强制改名为 `model.png`。若还提供透明 APNG 或黑底 WebM，应用可在 Spine 无法加载时使用它们作为降级资源。

## profile.json 示例

```json
{
  "id": "prts-char_172-default",
  "operator": {
    "id": "char_172_svrash",
    "name": "银灰",
    "outfitId": "default",
    "outfitName": "默认"
  },
  "actions": {
    "Relax": "Relax",
    "Move": "Move",
    "Sit": "Sit",
    "Sleep": "Sleep",
    "Interact": "Interact"
  },
  "files": {
    "defaultAsset": "build_char_172_svrash_default.png",
    "model": {
      "skeleton": "model.skel",
      "atlas": "model.atlas"
    },
    "processedActions": {
      "Relax": "relax.apng",
      "Move": "move.apng"
    },
    "fallbackActions": {
      "Relax": "relax.webm",
      "Move": "move.webm"
    }
  },
  "calibration": {
    "scale": 1,
    "groundOffset": 0,
    "headHitRatio": 0.3333
  }
}
```

## 字段规则

- `id`：全局唯一，只能使用字母、数字、点、下划线和连字符，最长 80 个字符。
- `operator`：四个字段均必填；其中 `id` 和 `outfitId` 应尽量保存上游素材站点的稳定 ID。
- `actions`：动作语义到实际 Spine 动画名的映射。`Move` 和 `Relax` 必填；`Sit`、`Sleep`、`Interact` 可省略。
- `files.defaultAsset`、`files.model.skeleton`、`files.model.atlas` 必填，且都必须是素材包内部的相对路径。
- `processedActions` 与 `fallbackActions` 可选；各项的键为 `Relax`、`Move`、`Sit`、`Sleep`、`Interact`。
- 不允许使用绝对路径或 `..` 路径；单个素材包总大小上限为 300 MB。

## 兼容性

- **完整兼容**：有 `Move`、`Relax`、`Sit`、`Sleep`、`Interact`；可使用全部现有行为。
- **基础兼容**：至少有 `Move` 和 `Relax`；缺失的坐下、睡眠、互动会回退为放松或被禁用。
- **不兼容**：缺少 `Move` 或 `Relax`，应用会拒绝导入。

从三杠菜单选择“导入素材包”即可导入。导入成功后，在“更换干员”中将其设置为左侧或右侧角色。请仅导入你有权使用和保存的素材。
